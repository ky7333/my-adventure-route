{
  "name": "my-adventure-route",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.2",
  "scripts": {
    "dev": "pnpm --filter @adventure/contracts build && pnpm --filter @adventure/scoring build && turbo run dev --parallel",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "docker:up": "docker compose -f infra/docker-compose.yml up -d",
    "docker:down": "docker compose -f infra/docker-compose.yml down",
    "db:migrate": "pnpm --filter @adventure/api prisma:migrate",
    "db:seed": "pnpm --filter @adventure/api prisma:seed",
    "fetch:osm": "bash infra/scripts/fetch-vermont-osm.sh"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^22.13.10",
    "eslint": "^9.22.0",
    "prettier": "^3.5.3",
    "turbo": "^2.5.3",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.45.0"
  }
}
